# Copy values into your shell. Do not commit real credentials.
DB_URL=jdbc:postgresql://localhost:5432/taskflow
DB_USERNAME=taskflow
DB_PASSWORD=taskflow

# Local development only. Use Flyway/Liquibase in production.
# JPA_DDL_AUTO=update

# taskflow.scheduler.enabled=true
# taskflow.scheduler.fixed-delay=1000
# taskflow.http.connect-timeout=2s
# taskflow.http.read-timeout=5s
